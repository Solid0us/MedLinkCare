import ContentLoadingSpinner from "@/animations/ContentLoadingSpinner";
import React from "react";

const Loading = () => {
  return <ContentLoadingSpinner text="Loading Messages" />;
};

export default Loading;
