"use client";
import ContentLoadingSpinner from "@/animations/ContentLoadingSpinner";
const Loading = () => {
  return <ContentLoadingSpinner text="Loading Appointments" />;
};

export default Loading;
